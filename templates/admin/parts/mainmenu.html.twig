{% block mainmenu %}
    <section id="header">
        <div class="container">
            <div class="row">
                <div class="col-12">

                    <h1>
                        <a href="{{ path('index') }}" id="logo">
                            <img src="{{ asset('img/logo.png') }}" alt="Space-Tactics Logo">
                        </a>
                    </h1>

                    <nav id="nav">
                        <a href="{{ path('index') }}">Support</a>
                        <a href="{{ path('news') }}">Spielerliste</a>
                        <a href="{{ path('index') }}">Zurück zum Spiel</a>
                        {% if app.user is not null %}
                            <a href="{{ path('app_logout') }}" class="text-danger">Logout</a>
                        {% endif %}
                    </nav>

                </div>
            </div>
        </div>
    </section>
{% endblock %}